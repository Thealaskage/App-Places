import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alaskage.app',
  appName: 'Ionic-Angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
